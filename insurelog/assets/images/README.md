# Drop real images here (same filenames)

| File | Use |
| --- | --- |
| `logo.png` | Site / favicon logo |
| `banner.jpg` | Homepage hero full-bleed background (**not shipped yet**) |
| `1.png` … `6.png` | Product screenshot carousel (optional) |

When `1.png`–`6.png` are missing, the landing page shows an empty-state note instead of broken image links.

## Banner

Waiting on the PolicyLog hero photo. Until then the homepage uses a logo-red wash (`.hero--no-banner`) with no `banner.jpg`.

## Logo

- From `UI/PolicyLog/logo.png` (deep red `#c01425`).
